# AI Collaboration Rules

## AI Principles

- **Scope（スコープ厳守）**: 会話の主題・タスク・ゴールをAIが勝手に変更しない。話題変更はユーザーが明示するか、AIの提案をユーザーが許可した場合のみ。
- **Uncertainty（不明点の扱い）**: 重要な情報不足や曖昧さは質問する。軽微な不足は合理的な仮定で補い、仮定を明示する。推測で断定しない。

## Pre-Coding Checklist

不明・未定の項目があれば**作業前に1回でまとめて**質問する。推測で進めない。

**確認必須：**
- ゴール（完了条件）
- 言語・FW・バージョン制約
- 新規 or 既存コード修正
- テストの要否
- 影響範囲

**確認不要（既存コードに合わせて進める）：**
- コードスタイル / ファイル配置 / 軽微な実装詳細

## Error & Debug Handling

- エラー発生時は**原因分析 → 修正方針説明 → 実装**の順で進める
- エラーログ・スタックトレースは必ず全文確認してから対応
- 推測で修正しない（必要なら既存コードを確認）
- デバッグ用の `console.log` / `print` 文は本番コードに残さない

## Work Stance

- 大きな変更前に方針を説明してから着手する
- 不要な依存追加禁止：既存の依存で解決できないか先に検討する

## dev-charter Modification Rules

採用先プロジェクト（dev-charter を `git subtree` で取り込んだプロジェクト）では、`docs/dev-charter/` 配下のファイルを**直接編集しない**。

- 変更が必要な場合は dev-charter リポジトリ本体に Issue を立て、`git subtree pull` でアップデートを取り込む
- `git subtree pull` によるアップデートのみ許可する
- 採用先プロジェクト固有のルールは、プロジェクトの `AI_CONTEXT.md` または専用ファイルに記載する

## Charter Lookup

不明点が憲章に関係する場合は**全ファイルを検索せず**、以下の手順で参照する：

1. `CHARTER_INDEX.md` を読み、該当トピックのファイルを特定する
2. 特定したファイル（原則 1〜2 件）のみを読む
3. 参照後にユーザーへ提案・確認を行う

推測で断定せず、憲章を参照してからユーザーに提案・質問する。

## AI Tool Responsibilities

複数 AI の利用を前提とし、標準担当を以下の通りとする。
これは各ツールの能力上限ではなく、主担当とレビュー経路を明確にするための運用上の分担である。

- **Claude Code**:
  - 要件整理・技術選定・アーキテクチャ設計
  - 機能実装・大規模なコード変更・リファクタリング
  - 実装に必要なテストの作成と実行

- **Codex**:
  - コードレビュー（正確性・セキュリティ・回帰・テスト不足の確認）
  - バグの原因調査と修正
  - コードレビューで検出した問題の修正と、修正後のテスト・差分確認

- **GitHub Copilot**:
  - ファイル一覧・横断検索・重複候補の抽出など、主担当 AI を補助する機械的なリポジトリ探索
  - Dependabot の Pull Request・アラートの調査、更新、コンフリクト解消
  - Pull Request の作成・更新、明示されたレビューコメントへの対応、ブランチ同期、意図が明確なマージコンフリクトの解消
  - GitHub Actions・CI の失敗原因調査、修正、再実行確認
  - Issue のトリアージ、ラベル・担当者・マイルストーンの設定
  - リポジトリ設定など GitHub 上の定型的な保守作業

- **Gemini CLI**:
  - README・ガイド・リリースノートなどの文章作成と更新
  - 翻訳・校正・用語統一
  - プライバシーポリシー・ストア説明文・審査用ドキュメントの整備

- **ローカル LLM（Ollama）**:
  - ファイル探索・検索結果の分類・定型的な情報抽出など、単純だが大量のコンテキストを消費する読み取り専用タスク

## Local LLM Delegation

- 接続先・モデル名は憲章やリポジトリへ記載せず、`OLLAMA_HOST`・`OLLAMA_MODEL` または AI ツールのローカル設定から取得する
- `OLLAMA_HOST` が設定されている場合、その送信先はユーザーが管理する信頼済みローカル LLM として扱い、このセクションで許可されたタスクは憲章上の個別確認なしで委任してよい
- AI ツールや実行環境が要求するネットワーク権限・承認はこの信頼設定より優先する。権限制約を迂回しない
- タスクの目的・対象範囲・出力形式が明確で、結果を容易に検証できる場合に限り委任する
- ファイル一覧、関連ファイル候補、検索結果の分類、反復パターンの抽出、ログやテスト結果の要約に使用してよい
- 設計・実装・コードレビュー・セキュリティ判断・法務判断・外部サービスへの操作は委任しない
- 必要最小限のデータだけを渡し、シークレット・認証情報・個人情報を送信しない
- 出力には根拠となるファイルパス・該当箇所・不確実な点を含めさせ、主担当 AI が実ファイルで検証する
- 接続失敗・タイムアウト・コンテキスト超過の場合は、主担当 AI が作業を継続する。ローカル LLM を必須依存にしない

## GitHub Operations

Issue を作成する場合は、必ずリポジトリオーナーを `assignee` に設定する。

```
gh issue create --title "..." --body "..." --assignee @me
```

> `@me` はトークンのオーナーに解決されるため、ユーザー名のハードコードは不要。

## Rules for Multi-AI Usage

- 機能開発は原則として **Claude Code が設計・実装し、Codex がレビューする**
- Codex は指摘だけでなく、依頼された範囲で修正と検証まで担当する
- Codex 自身が修正した変更は、Claude Code または人間が最終確認する。同じ AI だけで実装と最終承認を完結させない
- GitHub Copilot の担当作業がコード変更を含む場合は Codex がレビューする
- GitHub Copilot の探索結果は調査資料として扱い、主担当 AI が実ファイルで確認してから判断に使用する
- GitHub Copilot の担当作業で仕様・アーキテクチャ・データ移行・セキュリティ方針の判断が必要な場合は Claude Code または人間へ引き継ぐ
- Gemini CLI が作成した技術文書は、実装担当またはレビュー担当が技術的整合性を確認する
- ローカル LLM の出力は調査資料としてのみ扱い、主担当 AI または人間が検証してから使用する
- AI 間の引き継ぎでは、タスクの目的・変更範囲・変更ファイル・実行したテスト・既知の懸念を共有する
- 各 AI の出力は他の AI または人間が検証し、最終的な承認・マージは人間が行う
- 担当ツールを利用できない場合は別の AI に委任できる。その場合はタスクコンテキストに担当変更を明記する
