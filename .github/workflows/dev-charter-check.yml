name: Dev Charter

on:
  push:
    branches: [main]
  schedule:
    - cron: "5 3 * * 2"  # Every Tue 03:05 UTC
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write

jobs:
  check:
    uses: y-marui/dev-charter/.github/workflows/check-charter.yml@main
    with:
      prefix: docs/dev-charter
